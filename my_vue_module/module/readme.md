# 介绍
```
这是专门用来存储组件的文件夹
组件结构
-组件文件夹（组件名==组件文件夹名)
    -组件名.js(存放组件相关的代码，组件配使用一次，该内容就复制一次)
    -组件名.html
    -public.css（无论使用多少个组件，都只会加载这一份css)
    -public_before.js(无论使用多少个组件，都只会加载这一份js)
    -public_last.js(无论使用多少个组件，都只会加载这一份js)
```